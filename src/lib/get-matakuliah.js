import prisma from "./prisma";

export const findMataKuliah = async (kode) => {
    // const user = await getUser();
    // const kelas = user.kelas;
    // kelas.mahasiswa = prisma?.mahasiswa?.findMany({
    //   where:{
    //     kelas: 
    //   }
    // })
    // return kelas;
  };